# Agy Delegation

For non-trivial implementation, use `agy-subagent`.
Workflow: parent plans -> agy edits -> parent reviews diff/tests.

Do not delegate:
- architecture decisions
- security-sensitive decisions
- final judgment

Keep parent output compact. Do not print large code blocks unless requested.
