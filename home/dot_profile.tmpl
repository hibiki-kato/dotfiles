# Managed by chezmoi. Do not edit directly. Update via chezmoi, using OS/host-specific partitioning when needed.
{{ if eq .chezmoi.os "darwin" -}}
[ -r "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"
# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/hibiki/.lmstudio/bin"
{{ else if eq .chezmoi.os "linux" -}}
if command -v setxkbmap >/dev/null 2>&1 && [ -n "${DISPLAY:-}" ]; then
    # convert caps lock to ctrl
    setxkbmap -option ctrl:nocaps || true
fi

# >>> juliaup initialize >>>
# !! Contents within this block are managed by juliaup !!
case ":$PATH:" in
    *:/home/hibiki/.juliaup/bin:*)
        ;;
    *)
        export PATH=/home/hibiki/.juliaup/bin${PATH:+:${PATH}}
        ;;
esac
# <<< juliaup initialize <<<
[ -r "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"
{{ end -}}
